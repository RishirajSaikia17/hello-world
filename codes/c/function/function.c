#include<stdio.h>

void main()
{
    int a, b, x, y;
    a = 20;
    b = 10;
    printf("\nThe product is %d", x);
    printf("\nThe quotient is %d", y);
}

int multi(int a, int b)
{
    int x;
    x = a * b;
    return (x);
}

int div(int a, int b)
{
    int y;
    y = a / b;
    return (y);
}

